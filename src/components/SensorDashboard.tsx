/**
 * Candidate starting point.
 *
 * Implement the challenge in this component. The mock API is available at
 * `src/api/readings.ts` and the domain types are in `src/types.ts`.
 */
export function SensorDashboard() {
  return (
    <section className="challenge-placeholder" aria-labelledby="challenge-title">
      <h1 id="challenge-title">Sensor Dashboard</h1>
      <p>Implement the sensor readings experience here.</p>
    </section>
  );
}
